# Local development defaults. Copy to .env and override as needed.

POSTGRES_DB=nakama
POSTGRES_USER=postgres
POSTGRES_PASSWORD=localdb

VITE_NAKAMA_HOST=localhost
VITE_NAKAMA_PORT=7350
VITE_NAKAMA_SCHEME=http

# Nakama's client-visible server key for this local sample.
# Replace alongside nakama/local.yml before any shared or production deployment.
NAKAMA_CLIENT_PUBLIC=defaultkey

# Nakama origins baked into the Content-Security-Policy connect-src directive.
# For HTTPS deployments set both to the production endpoint, e.g.:
#   NAKAMA_CONNECT_SRC=https://game.example.com:7350 wss://game.example.com:7350
NAKAMA_CONNECT_SRC=http://localhost:7350 ws://localhost:7350
